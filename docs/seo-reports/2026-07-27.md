# RIGN daily SEO audit — 2026-07-27

## Score: 65/100

This score describes the 25-route live site during the 27 July run. The nine
pages built by the daily automation, including today's Leeds page, remain on
the cumulative feature branch and are not credited as live.

| Category | Score | Evidence |
|---|---:|---|
| Technical SEO | **10/15** | All 25 live sitemap routes and all 25 unique internal destinations returned 200, JSON-LD parsed, and a nonexistent route returned 404. Lighthouse scored 100 for SEO, accessibility, and best practices. Mobile performance was 85 with a 3.69s LCP, 2.62s FCP, 74ms TBT, and 0.0007 CLS; desktop scored 99 with a 0.98s LCP and 0 CLS. Every sitemap URL still uses the apex host and redirects once to `www`; field Core Web Vitals, Search Console crawl reports, and mobile-usability reports are unavailable. |
| On-page SEO | **12/15** | Every live sitemap route has a title, meta description, one H1, and at least one H2. All internal destinations resolve, and no rendered image lacks an `alt` attribute; four decorative images correctly use empty alt text. Four rendered titles exceed 60 characters, 11 descriptions fall outside the 120–160-character review range, and `/og-default.jpg` returns 404. |
| Structured data | **11/15** | Every crawled JSON-LD block parsed. AutomotiveBusiness, Service, FAQPage, BreadcrumbList, and Article are present where implemented. The visible `/faq` page still lacks FAQPage schema, while complete public NAP, opening hours, and verified profile links cannot be added without confirmed business data. |
| Content quality & AI-overview readiness | **9/15** | Service pillars answer their main questions early and use conversational FAQs. Live guides and town hubs remain brief; pricing placeholder language, the disabled contact form, and conflicting live PPF positioning weaken trust. The deeper branch-only pages are not yet credited. |
| Local SEO | **9/15** | All five original priority towns have live routes and consistent South Yorkshire positioning. The shared town pages remain shallow, Leeds is still only a hub mention on live, and Google Business Profile alignment plus complete public NAP data cannot be verified from this environment. |
| Brand/authority content | **7/10** | Four live guides cover local meets, drives, chrome delete, and service comparison with a restrained point of view. Two researched brand guides remain branch-only, and verified external authority/profile signals remain limited. |
| Page-to-page uniqueness | **7/15** | Live service pages have distinct titles and H1s, but the short shared town template remains the largest duplication risk. Today's five-word main-content comparison peaked at 23.65% between Rotherham and Doncaster. The low-similarity branch pages are not live yet. |

## Five highest-impact fixes

1. **Deploy the accumulated automation work.** Nine researched pages,
   corrected `www` URL generation, and deeper local content remain absent from
   the live site and sitemap.
2. **Improve mobile loading consistency.** Today's mobile Lighthouse score was
   85 with a 3.69s LCP and 2.62s FCP, while desktop remained fast at 99. Inspect
   the mobile hero image path and validate with repeated lab runs and field data.
3. **Remove conversion and trust blockers.** Enable the contact form, replace
   placeholder pricing, and reconcile the `/about` PPF statement with the
   active PPF service.
4. **Rewrite the remaining shared town pages.** Their short repeated structure
   still produces up to 23.65% five-word similarity and limited local
   decision-making value.
5. **Finish social and local entity signals.** Add a real default Open Graph
   image, use `www` directly in the live sitemap and robots declaration, and
   only add complete NAP, hours, profile links, or GBP details after Connor
   confirms them.

## Live performance and analytics check

- The live sitemap still contains 25 unique routes. All returned 200 after one
  apex-to-`www` redirect, all 25 unique internal destinations resolved, every
  JSON-LD block parsed, `robots.txt` returned 200, and a nonexistent route
  returned 404.
- Lighthouse 13.4.1 scored mobile performance 85, accessibility 100, best
  practices 100, and SEO 100. Mobile LCP was 3.69s, FCP 2.62s, TBT 74ms, speed
  index 2.62s, and CLS 0.0007. Desktop scored 99/100/100/100 with a 0.98s LCP,
  0.27s FCP, 0ms TBT, 0.54s speed index, and 0 CLS.
- Search Console impressions/clicks and GA4 `whatsapp_click` / `form_submit`
  events are not connected to this environment. No performance winner was
  inferred and the queue was not reordered.
- The formal light competitor check was completed on pass five and is next due
  around pass ten. Leeds research found established individual service
  providers, but no formal competitor rerun was due on this ninth pass.

## Duplicate check and build selection

- Before implementation, the cumulative branch contained 33 App Router pages
  and 33 `SITE_ROUTES` entries; the live sitemap contained the unchanged
  25-route baseline.
- `/areas-we-cover/leeds` was absent from the app, route registry, live sitemap,
  and existing-page inventory. Leeds appeared only as a brief hub-page mention,
  and the dedicated town page was the first unchecked queue item.
- Research supported distinct value beyond that mention and every existing town
  page: an appointment-suitability answer, a four-service problem framework,
  verified M621 context, a mobile-workspace check, and a useful photo-enquiry
  sequence. It does not claim that RIGN is based in Leeds.
- After implementation, all 34 App Router content pages, 34 `SITE_ROUTES`
  entries, and 34 local sitemap entries match.

## Pre-publish QA and self-score

- `npm run lint` and `npm run build` passed on Next.js 16.2.10. The Leeds route
  was statically generated.
- The local production page returned 200 with 1,041 visible words, one H1, a
  43-character rendered title, and a 141-character meta description.
- Service, AutomotiveBusiness, FAQPage, and BreadcrumbList JSON-LD parsed
  without errors. Four visible FAQs match the four schema questions.
- Every internal destination and the checked National Highways M621 source
  returned 200. The inherited PageShell WhatsApp CTA is visible and uses the
  existing tracked component.
- The highest five-word main-content Jaccard similarity was **1.87%** against
  `/what-we-do`, well below the live shared-town risk.
- Desktop, 375px mobile, and 768px tablet checks found no overflow, clipping,
  overlap, console errors, or missing sections. The closing CTA and FAQs are
  visible at each relevant layout.
- The page contains no dealership partnership claim, named endorsement,
  market-growth figure, invented statistic, guaranteed result, or claim that
  polishing, detailing, or PPF repairs damage.
- Self-score: **95/100** — uniqueness 24/25, local specificity 18/20,
  schema/meta/breadcrumbs 13/15, links 10/10, WhatsApp CTA 10/10,
  claims/statistics safety 10/10, responsive design 10/10. The existing broken
  default social image accounts for the metadata deduction.

## Sources checked

- `https://www.rign.uk`, all 25 live sitemap routes, `robots.txt`, and
  `sitemap.xml`
- National Highways, “M621 junctions 1 to 7 (completed),” checked live on 27
  July 2026
- Leeds City Council, “Connecting Leeds” and “Roadworks and closures,” checked
  live on 27 July 2026
- Current Leeds chrome delete, detailing, paint correction, and PPF search
  results, used to confirm that a generic package page would not be distinctive
- All 33 pre-build branch pages and the final 34-route local production build
