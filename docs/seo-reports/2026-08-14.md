# RIGN daily SEO audit — 2026-08-14

## Score: 64/100

This score describes the 25-route live site, not the 12 researched pages that
remain on the cumulative automation branch. The score is one point lower than
13 August because today's mobile lab LCP moved from 3.82s to 4.52s while the
intermittent sitemap failure continued.

| Category | Score | Evidence |
|---|---:|---|
| Technical SEO | **9/15** | All 25 known live routes and 25 unique internal destinations returned 200, and a deliberately invalid route returned 404. `robots.txt` returned 200, while repeated `sitemap.xml` requests alternated between 500 and 200; the successful response contained 25 apex-host URLs that each require a redirect to `www`. Lighthouse scored mobile performance 84 with a 4.52s LCP and desktop performance 99 with a 1.02s LCP; both measured zero CLS. Search Console crawl reports, field Core Web Vitals, and mobile-usability reports remain unavailable. |
| On-page SEO | **12/15** | Every live route has a title, meta description, one H1, at least one H2, and no image missing an `alt` attribute. Four titles are over 60 characters, 11 descriptions are outside 120–160 characters, and `/og-default.jpg` returns 404. All crawled internal destinations resolved. |
| Structured data | **11/15** | Every live JSON-LD block parsed. AutomotiveBusiness, Service, FAQPage, BreadcrumbList, and Article types are present where implemented. The live `/faq` route still has no FAQPage schema, while complete public NAP, opening hours, verified profiles, and GBP alignment remain unavailable and must not be invented. |
| Content quality & AI-overview readiness | **9/15** | Service pillars answer their main questions early and include conversational FAQs. Live guides and shared town pages remain brief; placeholder pricing, the disabled contact form, and conflicting live PPF wording remain trust limitations. Branch-only answer-first content is not credited until deployment. |
| Local SEO | **9/15** | The five priority towns have dedicated live routes and consistent South Yorkshire positioning. Shared town content remains shallow, the Leeds page remains branch-only, and Google Business Profile alignment cannot be checked from this environment. |
| Brand/authority content | **7/10** | Four live guides cover local meets, local drives, chrome delete, and a service comparison with a genuine point of view. Three more researched guides remain undeployed on the cumulative branch. |
| Page-to-page uniqueness | **7/15** | Live service pillars are distinct, but the short shared town-page template remains the clearest duplicate-content risk. The branch's deeper service-town pages, including today's 1.86%-similarity route, are not credited until deployed. |

## Five highest-impact fixes

1. **Deploy the accumulated automation work.** Twelve researched pages,
   direct-`www` canonical generation, and deeper local content are absent from
   the live site and live sitemap.
2. **Fix sitemap reliability and mobile LCP.** Identify why the sitemap
   alternates between 500 and 200, remove its apex-to-`www` redirect, and
   reduce the 4.52-second mobile hero/slideshow LCP.
3. **Remove conversion and trust blockers.** Enable the contact form, replace
   placeholder pricing, and reconcile the live `/about` PPF statement with the
   active PPF service.
4. **Rewrite the remaining shared town pages.** Their brief repeated structure
   remains the site's highest page-to-page uniqueness risk.
5. **Complete verified entity and sharing signals.** Add a real default Open
   Graph image, and only add full NAP, hours, social profiles, or GBP details
   after Connor confirms them.

## Live performance and analytics check

- The 25 known live content routes returned 200, as did 25 unique internal
  destinations. All crawled JSON-LD parsed, no image lacked an `alt`
  attribute, and a nonexistent route returned 404.
- `sitemap.xml` returned both 500 and 200 during this run. Its successful
  response contained 25 unique `https://rign.uk` URLs, so each entry still
  redirects to `www`. `robots.txt` returned 200 and still points to the apex
  sitemap URL. `/og-default.jpg` returned 404.
- Lighthouse 13.4.1 scored mobile performance 84, accessibility 100, best
  practices 100, and SEO 100. Mobile FCP was 0.92s, LCP 4.52s, TBT 63ms,
  speed index 1.26s, and CLS 0.
- Desktop scored performance 99 and 100 in the other three categories, with
  0.26s FCP, 1.02s LCP, 0ms TBT, 0.57s speed index, and zero CLS.
- No Search Console or GA4 connector is available in this environment.
  Impressions, clicks, `whatsapp_click`, and `form_submit` therefore could not
  be compared. No winner was inferred and the equal queue was not reordered.

## Competitor-check cadence

This is the twelfth logged automation pass. The formal competitor check ran on
pass ten and is next due around pass fifteen, so no broad competitor crawl was
performed today.

## Duplicate check and build selection

- Before implementation, the cumulative branch had 36 App Router content
  pages and 36 `SITE_ROUTES` entries. The intermittent live sitemap's
  successful response contained the unchanged 25-route baseline.
- `/services/paint-protection-film/barnsley` was absent from the app, live
  sitemap, and existing-page inventory. It is a valid service-town route
  rather than a duplicate of the PPF pillar, Barnsley hub, or Chesterfield PPF
  page.
- Research checked Barnsley Council's current town-centre and borough parking
  information and National Highways' Southern Trans-Pennine corridor
  overview. The page uses those facts to distinguish parking-contact briefs
  from forward-facing road-use briefs without claiming either proves that a
  particular vehicle needs PPF.
- The new copy adds a contact-point decision method, coverage-edge limits,
  transparent-film preparation boundaries, a small-job photo brief, and
  mobile-workspace checks. After implementation, all 37 App Router content
  pages, 37 `SITE_ROUTES` entries, and 37 local sitemap entries match. The
  final slug appears once in the app and once in `SITE_ROUTES`; the PPF pillar
  and Barnsley hub now route their Barnsley links to the dedicated page.

## Pre-publish QA and self-score

- `npm run lint` and `npm run build` passed on Next.js 16.2.10. The new route
  was statically generated.
- The local production route returned 200 with 1,164 visible words, one H1,
  ten H2s, a 52-character rendered title, and a 149-character description.
- Service, AutomotiveBusiness, FAQPage, and BreadcrumbList page schemas parsed.
  All required fields were present, and all four schema questions matched the
  four visible FAQs. The shared JSON-LD renderer now applies the escaping
  recommended by the installed Next.js guide.
- Every HTTP(S) link rendered from the page resolved, including the three
  contextual internal links and both official sources. The canonical and
  sitemap URL use `https://www.rign.uk`, and the closing CTA uses the existing
  tracked WhatsApp pattern in `PageShell`.
- Five-word main-content similarity peaked at **1.86%** against the
  Chesterfield PPF page, well below the inherited shared-town risk.
- Desktop, 375px mobile, and 768px tablet browser checks found no overflow,
  clipping, overlap, missing section, broken styling, or console error. The
  three contextual links, two source links, and existing WhatsApp CTA all
  behaved as intended without submitting a message.
- The page contains no named dealership partnership, market-growth figure,
  fabricated statistic, guaranteed protection claim, or suggestion that PPF
  repairs missing or unstable paint.
- Self-score: **96/100** — uniqueness 24/25, local specificity 19/20,
  schema/meta/breadcrumbs 13/15, links 10/10, WhatsApp CTA 10/10,
  claims/statistics safety 10/10, responsive design 10/10. The inherited
  missing default social image accounts for the metadata deduction.

## Sources checked

- `https://www.rign.uk`, all 25 known live routes, `robots.txt`, intermittent
  `sitemap.xml`, and `/og-default.jpg`
- Barnsley Metropolitan Borough Council, “Car parks and on-street parking,”
  checked 14 August 2026
- National Highways, “Southern Trans-Pennine improvements,” checked 14 August
  2026
- All 36 pre-build branch routes and the final 37-route local production build
