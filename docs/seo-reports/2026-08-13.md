# RIGN daily SEO audit — 2026-08-13

## Score: 65/100

This score describes the 25-route live site, not the 11 researched pages that
remain on the cumulative automation branch. The score is held level with the
previous audit: mobile lab performance improved, but the first two live sitemap
requests returned HTTP 500 before repeated checks returned 200.

| Category | Score | Evidence |
|---|---:|---|
| Technical SEO | **10/15** | All 25 known live routes, 25 unique internal destinations, `robots.txt`, and the recovered `sitemap.xml` returned 200; a deliberately invalid route returned 404. Every sitemap URL still makes one apex-to-`www` redirect. Lighthouse scored mobile performance 89 with a 3.82s LCP and desktop performance 100 with a 0.63s LCP; both measured zero CLS. The sitemap's transient 500 responses and unavailable field Core Web Vitals, Search Console crawl reports, and mobile-usability reports prevent a higher score. |
| On-page SEO | **12/15** | Every live route has a title, meta description, one H1, at least one H2, and no image missing an `alt` attribute. Five titles remain over 60 characters, 11 descriptions remain outside 120–160 characters, and `/og-default.jpg` returns 404. All crawled internal links resolved. |
| Structured data | **11/15** | Every crawled JSON-LD block parsed. AutomotiveBusiness, Service, FAQPage, BreadcrumbList, and Article types are present where implemented. The live `/faq` page still lacks FAQPage schema, and complete public NAP, opening hours, verified profiles, and GBP alignment remain unavailable and must not be invented. |
| Content quality & AI-overview readiness | **9/15** | Service pillars answer their main questions early and include conversational FAQs. Live guides and shared town pages remain brief; placeholder pricing, the disabled contact form, and conflicting live PPF wording remain trust limitations. Branch-only answer-first content is not credited until deployment. |
| Local SEO | **9/15** | The five priority towns have dedicated live routes and consistent South Yorkshire positioning. Shared town content remains shallow, the Leeds page remains branch-only, and Google Business Profile alignment cannot be checked from this environment. |
| Brand/authority content | **7/10** | Four live guides cover local meets, local drives, chrome delete, and a service comparison with a genuine point of view. Three additional researched guides remain undeployed on the cumulative branch. |
| Page-to-page uniqueness | **7/15** | Live service pillars are distinct, but the short shared town-page template remains the highest duplicate-content risk. The branch's deeper pages, including today's 1.59%-similarity route, are not credited until deployed. |

## Five highest-impact fixes

1. **Deploy the accumulated automation work.** Eleven researched pages,
   direct-`www` canonical generation, and deeper local content are absent from
   the live site and live sitemap.
2. **Investigate sitemap reliability and mobile LCP.** Confirm why initial
   sitemap requests returned 500 and reduce the mobile hero/slideshow LCP,
   which remains above the 2.5-second good threshold in today's lab run.
3. **Remove conversion and trust blockers.** Enable the contact form, replace
   placeholder pricing, and reconcile the live `/about` PPF statement with the
   active PPF service.
4. **Rewrite the remaining shared town pages.** Their brief repeated structure
   remains the site's clearest uniqueness and local-depth risk.
5. **Complete verified entity and share signals.** Add a real default Open
   Graph image and only add full NAP, hours, social profiles, or GBP details
   after Connor confirms them.

## Live performance and analytics check

- The live sitemap recovered to HTTP 200 and contains 25 unique apex-host URLs.
  All 25 known content routes and 25 unique internal destinations returned 200
  after redirects, all crawled JSON-LD parsed, and a nonexistent route returned
  404. `/og-default.jpg` still returned 404.
- Lighthouse 13.4.1 scored mobile performance 89, accessibility 100, best
  practices 100, and SEO 100. Mobile FCP was 0.96s, LCP 3.82s, TBT 53ms, speed
  index 1.23s, and CLS 0.
- Desktop scored 100 in all four categories, with 0.29s FCP, 0.63s LCP, 0ms
  TBT, 0.91s speed index, and CLS 0.
- Search Console impressions/clicks and GA4 `whatsapp_click` / `form_submit`
  events are not connected to this environment. No performance winner was
  inferred and the equal queue was not reordered.

## Competitor-check cadence

This is the eleventh logged automation pass. The formal competitor check ran on
pass ten and is next due around pass fifteen, so no new broad competitor crawl
was performed. Today's content research did confirm active Rotherham machine-
polishing pages, which reinforces the need for RIGN's distinct diagnosis-led
answer rather than changing the equal rotation.

## Duplicate check and build selection

- Before implementation, the cumulative branch had 35 App Router content pages
  and 35 `SITE_ROUTES` entries; the recovered live sitemap had 25 routes.
- `/services/paint-correction/rotherham` was absent from the app, live sitemap,
  and existing-page inventory. It is a valid combination route rather than a
  duplicate of the paint-correction pillar or Rotherham town hub.
- Research covered current Rotherham machine-polishing results and Rotherham
  Council's published A630, A633, and A629 commuter-route context. The new page
  adds a four-signal paint-reading method, test-section decision logic,
  previous-paint history, mobile-workspace checks, and aftercare boundaries.
- After implementation, all 36 App Router content pages, 36 `SITE_ROUTES`
  entries, and 36 local sitemap entries match. The final slug appears once.

## Pre-publish QA and self-score

- `npm run lint` and `npm run build` passed on Next.js 16.2.10. The new route
  was statically generated.
- The local production route returned 200 with 1,235 visible words, one H1, 11
  H2s, a 53-character rendered title, and a 147-character description.
- Service, AutomotiveBusiness, FAQPage, and BreadcrumbList page schemas parsed.
  All required fields were present, and all four schema questions matched the
  four visible FAQs.
- All 19 internal destinations rendered from the page and the checked Rotherham
  Council source resolved. The canonical and sitemap URL use
  `https://www.rign.uk`, and the closing CTA uses the existing tracked WhatsApp
  pattern in `PageShell`.
- Five-word main-content similarity peaked at **1.59%** against the Barnsley
  paint-correction page. This is well below the inherited shared-town risk.
- Desktop, 375px mobile, and 768px tablet checks found no overflow, clipping,
  overlap, missing section, broken styling, or console error.
- The page contains no named dealership partnership, market-growth figure,
  fabricated statistic, guaranteed result, or claim that polishing can replace
  missing paint.
- Self-score: **96/100** — uniqueness 24/25, local specificity 19/20,
  schema/meta/breadcrumbs 13/15, links 10/10, WhatsApp CTA 10/10,
  claims/statistics safety 10/10, responsive design 10/10. The inherited
  missing default social image accounts for the metadata deduction.

## Sources checked

- `https://www.rign.uk`, all 25 known live routes, `robots.txt`, recovered
  `sitemap.xml`, and `/og-default.jpg`
- Rotherham Metropolitan Borough Council, “Clean Air Rotherham — What is Being
  Proposed in Rotherham,” checked 13 August 2026
- Current search results and pages for paint correction and machine polishing
  in Rotherham, checked 13 August 2026
- All 35 pre-build branch routes and the final 36-route local production build
